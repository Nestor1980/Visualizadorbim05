services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app/src
      - ./public/workers:/app/public/workers
      - ./index.html:/app/index.html
    restart: unless-stopped
